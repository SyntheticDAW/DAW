# Compile with asc (AssemblyScript) with SIMD and imported memory
asc example.asc.ts -b example.wasm --enable simd --importMemory

# Move the output file to the target folder, overwriting if it exists
mv -f ./example.wasm ../../../public/example.wasm
